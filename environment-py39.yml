name: emb3rs-market-module
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip=
  - colorama=
  - cvxpy=
  - matplotlib=
  - numpy=
  - pandas=
  - pyscipopt=3.5.0
  - scipy=
  - seaborn=
  # for pypi packages (uncomment only if there are packages)
  # - pip:
  #     - numpy==
